# PRD: CGV 용산 IMAX 영화 예매 오픈 알림 시스템

## 1. 프로젝트 개요

### 1.1 목적
CGV 용산 IMAX 상영관의 특정 영화 예매 오픈을 자동으로 감지하여 사용자에게 실시간으로 알림을 제공하는 웹 기반 모니터링 시스템

### 1.2 사용자
- **단일 사용자용** (개인 사용 목적)
- 필요시 알림 수신 이메일만 추가하여 다른 사람도 이용 가능

### 1.3 목표
- 영화 예매 오픈 시점을 놓치지 않고 즉시 알림 받기
- 웹 UI를 통한 간단한 영화 선택 및 상태 확인
- 이메일 알림 및 웹 대시보드 표시

## 2. 핵심 기능 (Core Features)

### 2.1 웹 UI (MVP)
**메인 대시보드**
- [ ] 현재 감시 중인 영화 정보 표시 (제목만)
- [ ] 감시 상태 표시 (감시 중/중지)
- [ ] 감시 시작/중지 버튼
- [ ] 마지막 체크 시간 표시
- [ ] 예매 오픈 감지 시 알림 메시지 표시 (예매 링크 포함)

**영화 선택**
- [ ] CGV 용산 IMAX 상영 예정 영화 목록 표시 (제목 리스트)
- [ ] 한 개의 영화만 선택 가능 (라디오 버튼)
- [ ] 선택 확인 버튼

**설정**
- [ ] 알림 받을 이메일 주소 입력/수정 (여러 개 가능)
- [ ] 테스트 이메일 발송 버튼
- [ ] 저장 버튼

### 2.2 나중에 구현할 UI 기능
- [ ] 영화 검색 기능
- [ ] 영화별 포스터, 개봉일 등 상세 정보 표시
- [ ] 알림 히스토리 페이지
- [ ] 모니터링 체크 간격 설정
- [ ] 다중 영화 감시

### 2.3 모니터링 시스템 (MVP)
**크롤링 엔진**
- [ ] CGV 웹사이트 주기적 크롤링 (CGV 봇 정책 준수하면서 빠른 주기)
- [ ] 용산 IMAX 상영관 정보 파싱
- [ ] 예매 오픈 상태 감지 로직
- [ ] 에러 핸들링 및 재시도 로직
- [ ] 중복 알림 방지 로직
- [ ] 수동 중지 전까지 계속 감시 (자동 중지 없음)

**알림 발송**
- [ ] 예매 오픈 감지 시 즉시 이메일 발송 (여러 이메일 주소 지원)
- [ ] 이메일 내용: 영화 제목, 예매 링크
- [ ] 웹 대시보드에 알림 메시지 표시

### 2.4 데이터 관리 (MVP)
**간단한 설정 저장**
- [ ] 감시 중인 영화 정보 저장 (1개)
- [ ] 이메일 주소 목록 저장
- [ ] 감시 상태 저장 (시작/중지)
- [ ] 마지막 체크 시간 저장

**나중에 구현할 데이터 기능**
- [ ] 알림 히스토리 DB 저장 및 조회
- [ ] 크롤링 로그 저장

## 3. 기술 스택

### 3.1 백엔드 (MVP)
- **언어**: Python 3.10+
- **웹 프레임워크**: Flask (간단하고 빠른 개발)
- **크롤링**:
  - requests + BeautifulSoup4 (정적 페이지 우선)
  - Selenium (동적 페이지 필요시만)
- **스케줄링**: APScheduler (간단한 백그라운드 작업)
- **이메일**: smtplib (Gmail SMTP 사용)
- **데이터 저장**: JSON 파일 (MVP용, 나중에 SQLite로 전환)

### 3.2 프론트엔드 (MVP)
- **기본 HTML + JavaScript** (React 없이 시작, 빠른 개발)
- **스타일**: 간단한 CSS 또는 Bootstrap
- **HTTP 요청**: Fetch API
- **나중에 업그레이드**: React + Material-UI

### 3.3 배포
- **웹서버**: Flask 내장 서버 → Gunicorn + Nginx (프로덕션)
- **프로세스 관리**: nohup/screen (개발) → Systemd/Supervisor (프로덕션)
- **호스팅**: AWS EC2, DigitalOcean, Heroku, 또는 개인 서버
- **도메인/SSL**: 선택사항 (나중에 추가 가능)

## 4. 시스템 아키텍처 (MVP)

```
┌─────────────────────────────────────────────────────┐
│                   Web Browser                        │
│            (HTML + JavaScript)                       │
└──────────────────┬──────────────────────────────────┘
                   │ HTTP (Fetch API)
┌──────────────────┴──────────────────────────────────┐
│                  Flask Server                        │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │   API       │  │  CGV         │  │  Email     │ │
│  │  Endpoints  │  │  Crawler     │  │  Sender    │ │
│  └─────────────┘  └──────────────┘  └────────────┘ │
│         │                │                  │        │
│  ┌──────┴────────────────┴──────────────────┴─────┐ │
│  │         Background Scheduler                    │ │
│  │            (APScheduler)                        │ │
│  └──────────────────────┬──────────────────────────┘ │
└─────────────────────────┼──────────────────────────┘
                          │
                ┌─────────┴──────────┐
                │   JSON Files       │
                │  (config.json)     │
                └────────────────────┘
```

## 5. 데이터 저장 구조 (MVP - JSON 파일)

### 5.1 config.json
```json
{
  "monitoring": {
    "movie_title": "인터스텔라",
    "is_running": false,
    "last_check_time": "2025-11-08 15:30:00",
    "last_notification": null
  },
  "email": {
    "recipients": [
      "user@example.com",
      "friend@example.com"
    ],
    "smtp_server": "smtp.gmail.com",
    "smtp_port": 587,
    "sender_email": "your-email@gmail.com",
    "sender_password": "your-app-password"
  },
  "crawler": {
    "check_interval_seconds": 180,
    "theater": "용산 IMAX",
    "last_crawl_status": "success"
  }
}
```

### 5.2 나중에 구현할 데이터베이스 (SQLite/PostgreSQL)
**notifications 테이블** (알림 히스토리용)
```sql
- id: INTEGER PRIMARY KEY
- movie_title: VARCHAR(200)
- booking_url: TEXT
- sent_at: TIMESTAMP
- email_sent_success: BOOLEAN
```

## 6. API 엔드포인트 (MVP)

### 6.1 영화 관리
```
GET    /api/movies              # CGV 용산 IMAX 영화 목록 조회
POST   /api/movies/select       # 감시할 영화 선택
```

### 6.2 모니터링 제어
```
GET    /api/monitoring/status   # 현재 감시 상태 조회
POST   /api/monitoring/start    # 감시 시작
POST   /api/monitoring/stop     # 감시 중지
```

### 6.3 설정
```
GET    /api/settings            # 설정 조회
PUT    /api/settings/email      # 이메일 주소 업데이트
POST   /api/settings/email/test # 테스트 이메일 발송
```

### 6.4 나중에 추가할 엔드포인트
```
GET    /api/notifications       # 알림 히스토리 조회
DELETE /api/notifications/{id}  # 알림 삭제
```

## 7. UI/UX 디자인 (MVP - 단일 페이지)

### 7.1 메인 페이지 (단일 페이지 앱)

**상단: 헤더**
- 제목: "CGV 용산 IMAX 예매 알림"
- 현재 시간 표시

**섹션 1: 감시 상태**
- 감시 중인 영화: [영화 제목]
- 상태 표시: 감시 중 (초록) / 중지됨 (회색)
- 마지막 체크: [시간]
- 버튼: [감시 시작] / [감시 중지]

**섹션 2: 영화 선택**
- 드롭다운 또는 라디오 버튼 리스트
- CGV 용산 IMAX 상영 예정 영화 목록
- [선택 저장] 버튼

**섹션 3: 이메일 설정**
- 이메일 주소 입력 필드 (여러 개 추가 가능)
- [이메일 추가] 버튼
- 등록된 이메일 목록 (삭제 버튼 포함)
- [테스트 이메일 발송] 버튼
- [저장] 버튼

**섹션 4: 최근 알림 (있는 경우)**
- 알림 메시지 박스
- 예매 링크 버튼

### 7.2 디자인
- 간단하고 깔끔한 Bootstrap 스타일
- 데스크톱 우선 (모바일은 나중에)

## 8. 이메일 알림 형식

**제목**: [CGV 알림] {영화명} 예매 오픈!

**본문**:
```
안녕하세요,

CGV 용산 IMAX에서 다음 영화의 예매가 오픈되었습니다!

🎬 영화: {영화 제목}
📍 극장: CGV 용산 IMAX
🎫 예매 링크: {예매 URL}

지금 바로 예매하세요!

---
CGV 예매 알림 시스템
감지 시간: {YYYY-MM-DD HH:MM:SS}
```

## 9. 개발 단계

### Phase 1: MVP (최소 기능 제품) - 빠른 프로토타입
**백엔드**
- [ ] Flask 기본 서버 구축
- [ ] CGV 크롤러 구현 (용산 IMAX 전용)
- [ ] 예매 오픈 감지 로직
- [ ] 이메일 발송 기능 (Gmail SMTP)
- [ ] APScheduler로 자동 감시
- [ ] JSON 파일 설정 저장/로드
- [ ] REST API 구현 (영화 목록, 감시 제어, 설정)

**프론트엔드**
- [ ] 단일 HTML 페이지
- [ ] Bootstrap으로 간단한 UI
- [ ] JavaScript로 API 호출
- [ ] 영화 선택 기능
- [ ] 감시 시작/중지 버튼
- [ ] 이메일 설정 기능
- [ ] 테스트 이메일 발송

**테스트**
- [ ] 로컬 환경에서 실행 테스트
- [ ] 이메일 발송 테스트
- [ ] 크롤러 동작 확인

**예상 기간**: 3-5일

### Phase 2: 배포 및 안정화
- [ ] 에러 핸들링 강화
- [ ] 로깅 시스템 추가
- [ ] 크롤링 실패 시 재시도 로직
- [ ] 중복 알림 방지
- [ ] 웹서버 배포 (Gunicorn + Nginx)
- [ ] 프로세스 관리 (Systemd)
- [ ] 실제 서버에서 24시간 가동 테스트

**예상 기간**: 2-3일

### Phase 3: 추가 기능 (우선순위 순)
- [ ] 알림 히스토리 (SQLite DB 연동)
- [ ] 영화 상세 정보 표시 (포스터, 개봉일)
- [ ] 다중 영화 감시
- [ ] 다른 극장 지원 (용산 일반관, 강남 IMAX 등)
- [ ] UI/UX 개선 (React로 전환)
- [ ] 모니터링 간격 설정 기능
- [ ] 모바일 반응형 디자인

**예상 기간**: 각 기능당 1-2일

### Phase 4: 고급 기능 (선택)
- [ ] 텔레그램 봇 알림 추가
- [ ] 카카오톡 알림 (복잡함)
- [ ] 다른 극장 체인 지원 (메가박스, 롯데시네마)
- [ ] 사용자 인증 (다중 사용자 지원)
- [ ] 모바일 앱 (PWA)

**예상 기간**: 각 기능당 2-7일

## 10. 고려사항 및 제약

### 10.1 법적/윤리적 고려사항
- CGV 이용약관 및 robots.txt 준수
- 과도한 크롤링으로 인한 서버 부하 방지
- 개인 사용 목적으로만 사용
- 상업적 이용 금지

### 10.2 기술적 제약
- CGV 웹사이트 구조 변경 시 크롤러 수정 필요
- IP 차단 위험 (적절한 간격 유지 필요)
- 동적 페이지 로딩 시 Selenium 필요 (속도 저하)
- 이메일 발송 제한 (일일 발송량)

### 10.3 보안
- 이메일 계정 정보 암호화 저장
- 환경변수로 민감 정보 관리
- HTTPS 필수
- CSRF 토큰 사용
- SQL Injection 방지

### 10.4 성능
- 크롤링 간격: CGV 봇 정책 준수하되 빠른 주기 (예: 3분)
- 캐싱 전략 (영화 목록 등)
- 비동기 처리 (크롤링, 이메일 발송)
- User-Agent 설정으로 정상 브라우저처럼 동작

## 11. 성공 지표

- [ ] 예매 오픈 감지 정확도: 95% 이상
- [ ] 알림 발송 지연시간: 5분 이내
- [ ] 시스템 가동률: 99% 이상
- [ ] 이메일 전송 성공률: 95% 이상
- [ ] 크롤링 성공률: 90% 이상

## 12. 리스크 및 대응 방안

| 리스크 | 영향도 | 대응 방안 |
|--------|--------|-----------|
| CGV 웹사이트 구조 변경 | 높음 | 모니터링 및 빠른 업데이트, 에러 알림 |
| IP 차단 | 중간 | 적절한 간격 유지, User-Agent 설정, 프록시 고려 |
| 이메일 발송 실패 | 중간 | 재시도 로직, 대체 이메일 서비스 |
| 서버 다운타임 | 높음 | 헬스 체크, 자동 재시작, 모니터링 알림 |
| 데이터베이스 오류 | 중간 | 정기 백업, 트랜잭션 관리 |

## 13. 참고 자료

- CGV 웹사이트: https://www.cgv.co.kr/
- 크롤링 관련 법률: 저작권법, 정보통신망법
- 이메일 발송: Gmail SMTP, SendGrid API
- 배포 가이드: Flask/FastAPI 프로덕션 배포 문서

## 14. 향후 확장 가능성

### 우선순위 높음
1. **다른 CGV 극장 지원**: 용산 일반관, 강남 IMAX, 왕십리 등
2. **다중 영화 감시**: 여러 영화 동시 감시
3. **알림 히스토리**: DB 기반 히스토리 관리

### 우선순위 중간
4. **다른 극장 체인**: 메가박스, 롯데시네마
5. **텔레그램 알림**: 이메일 외 추가 알림 수단
6. **특정 좌석 알림**: 원하는 좌석 구역 감시

### 우선순위 낮음
7. **가격 추적**: 할인 이벤트 알림
8. **단체 관람 매칭**: 소셜 기능
9. **모바일 앱**: React Native 앱
10. **AI 추천**: 선호도 기반 추천

---

**문서 버전**: 2.0 (MVP 범위 조정)
**작성일**: 2025-11-08
**최종 수정일**: 2025-11-08
**변경 사항**:
- 단일 사용자용으로 명시
- 한 개 영화만 감시하도록 단순화
- JSON 파일 기반 설정 (DB는 나중에)
- HTML + JavaScript로 빠른 프로토타입
- Phase 1 예상 기간: 3-5일로 단축
