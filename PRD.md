# PRD: CGV 용산 IMAX 영화 예매 오픈 알림 시스템

## 1. 프로젝트 개요

### 1.1 목적
CGV 용산 IMAX 상영관의 특정 영화 예매 오픈을 자동으로 감지하여 사용자에게 실시간으로 알림을 제공하는 웹 기반 모니터링 시스템

### 1.2 목표
- 영화 예매 오픈 시점을 놓치지 않고 즉시 알림 받기
- 웹 UI를 통한 편리한 영화 선택 및 관리
- 이메일과 웹 대시보드를 통한 이중 알림 시스템

## 2. 핵심 기능 (Core Features)

### 2.1 웹 UI
**영화 선택 및 관리**
- [ ] CGV 용산 IMAX 상영 예정 영화 목록 표시
- [ ] 영화 검색 기능
- [ ] 감시할 영화 선택/해제 (체크박스 또는 토글)
- [ ] 선택한 영화 목록 실시간 확인
- [ ] 영화별 포스터, 제목, 개봉일 정보 표시

**대시보드**
- [ ] 현재 모니터링 중인 영화 상태 표시
- [ ] 최근 감지된 예매 오픈 내역
- [ ] 각 알림 항목에 예매 링크 포함
- [ ] 마지막 체크 시간 표시
- [ ] 시스템 상태 표시 (실행 중/중지)

**설정 페이지**
- [ ] 알림 받을 이메일 주소 설정
- [ ] 모니터링 체크 간격 설정 (5분/10분/30분)
- [ ] 테스트 이메일 발송 기능
- [ ] 알림 히스토리 보관 기간 설정

### 2.2 모니터링 시스템
**크롤링 엔진**
- [ ] CGV 웹사이트 주기적 크롤링
- [ ] 용산 IMAX 상영관 정보 파싱
- [ ] 예매 오픈 상태 감지 로직
- [ ] 에러 핸들링 및 재시도 로직
- [ ] 크롤링 간격 준수 (서버 부하 방지)

**알림 발송**
- [ ] 예매 오픈 감지 시 즉시 이메일 발송
- [ ] 이메일 내용: 영화 제목, 상영 시간, 예매 링크
- [ ] 웹 대시보드에 실시간 알림 표시
- [ ] 중복 알림 방지 로직

### 2.3 데이터 관리
**데이터베이스**
- [ ] 감시 중인 영화 목록 저장
- [ ] 알림 히스토리 저장
- [ ] 사용자 설정 저장 (이메일, 체크 간격 등)
- [ ] 마지막 크롤링 상태 저장

**알림 히스토리**
- [ ] 날짜/시간, 영화명, 예매 링크 기록
- [ ] UI에서 히스토리 조회 가능
- [ ] 히스토리 삭제 기능

## 3. 기술 스택

### 3.1 백엔드
- **언어**: Python 3.10+
- **웹 프레임워크**: Flask 또는 FastAPI
- **크롤링**:
  - BeautifulSoup4 (정적 페이지)
  - Selenium (동적 페이지, 필요시)
- **스케줄링**: APScheduler 또는 Celery
- **이메일**: smtplib 또는 SendGrid API
- **데이터베이스**: SQLite (개발) / PostgreSQL (프로덕션)
- **ORM**: SQLAlchemy

### 3.2 프론트엔드
- **프레임워크**: React 또는 Vue.js (추천: React)
- **UI 라이브러리**: Material-UI 또는 Tailwind CSS
- **HTTP 클라이언트**: Axios
- **상태 관리**: Context API 또는 Redux (필요시)
- **실시간 업데이트**: WebSocket 또는 Server-Sent Events (SSE)

### 3.3 배포
- **웹서버**: Gunicorn + Nginx
- **프로세스 관리**: Systemd 또는 Supervisor
- **호스팅**: AWS EC2, DigitalOcean, Heroku 등
- **도메인/SSL**: Let's Encrypt (무료 SSL)

## 4. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────┐
│                   Web Browser                        │
│              (React Frontend UI)                     │
└──────────────────┬──────────────────────────────────┘
                   │ HTTP/WebSocket
┌──────────────────┴──────────────────────────────────┐
│              Flask/FastAPI Server                    │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │   API       │  │  CGV         │  │  Email     │ │
│  │  Endpoints  │  │  Crawler     │  │  Sender    │ │
│  └─────────────┘  └──────────────┘  └────────────┘ │
│         │                │                  │        │
│  ┌──────┴────────────────┴──────────────────┴─────┐ │
│  │           Background Scheduler                  │ │
│  │            (APScheduler/Celery)                 │ │
│  └──────────────────────┬──────────────────────────┘ │
└─────────────────────────┼──────────────────────────┘
                          │
                ┌─────────┴──────────┐
                │   Database         │
                │  (SQLite/PostgreSQL)│
                └────────────────────┘
```

## 5. 데이터베이스 스키마

### 5.1 테이블 설계

**movies (감시 중인 영화)**
```sql
- id: INTEGER PRIMARY KEY
- cgv_movie_id: VARCHAR(50) UNIQUE
- title: VARCHAR(200)
- poster_url: TEXT
- release_date: DATE
- is_monitoring: BOOLEAN DEFAULT TRUE
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

**notifications (알림 히스토리)**
```sql
- id: INTEGER PRIMARY KEY
- movie_id: INTEGER FOREIGN KEY
- notification_type: VARCHAR(20) (email/web)
- booking_url: TEXT
- showtime_info: TEXT
- sent_at: TIMESTAMP
- email_sent_success: BOOLEAN
```

**settings (시스템 설정)**
```sql
- id: INTEGER PRIMARY KEY
- key: VARCHAR(50) UNIQUE
- value: TEXT
- updated_at: TIMESTAMP
```

설정 예시:
- email_address: 알림 받을 이메일
- check_interval: 체크 간격 (분)
- last_crawl_time: 마지막 크롤링 시간
- crawler_status: 크롤러 상태 (running/stopped)

## 6. API 엔드포인트

### 6.1 영화 관리
```
GET    /api/movies              # 전체 영화 목록 조회
GET    /api/movies/monitoring   # 감시 중인 영화 목록
POST   /api/movies/{id}/monitor # 영화 감시 시작
DELETE /api/movies/{id}/monitor # 영화 감시 중지
```

### 6.2 알림 관리
```
GET    /api/notifications       # 알림 히스토리 조회
GET    /api/notifications/latest # 최근 알림
POST   /api/notifications/test  # 테스트 이메일 발송
DELETE /api/notifications/{id}  # 알림 삭제
```

### 6.3 설정
```
GET    /api/settings            # 전체 설정 조회
PUT    /api/settings            # 설정 업데이트
POST   /api/settings/email/test # 이메일 설정 테스트
```

### 6.4 시스템
```
GET    /api/system/status       # 시스템 상태 조회
POST   /api/system/crawler/start  # 크롤러 시작
POST   /api/system/crawler/stop   # 크롤러 중지
GET    /api/system/crawler/status # 크롤러 상태
```

## 7. UI/UX 디자인

### 7.1 주요 페이지

**대시보드 (메인 페이지)**
- 헤더: 로고, 시스템 상태 표시
- 좌측: 감시 중인 영화 카드 (포스터, 제목, 상태)
- 우측: 최근 알림 타임라인
- 하단: 시스템 통계 (총 감시 영화 수, 총 알림 수 등)

**영화 선택 페이지**
- 검색바
- 영화 그리드 레이아웃 (포스터 썸네일)
- 각 영화 카드에 "감시 시작/중지" 토글
- 필터: 개봉 예정/개봉 중

**알림 히스토리 페이지**
- 테이블 형식 또는 타임라인
- 각 항목: 시간, 영화명, 예매 링크 버튼
- 페이지네이션

**설정 페이지**
- 이메일 주소 입력
- 체크 간격 선택 (드롭다운)
- 테스트 이메일 발송 버튼
- 저장 버튼

### 7.2 반응형 디자인
- 모바일, 태블릿, 데스크톱 대응
- 모바일에서도 편리한 영화 선택 UI

## 8. 이메일 알림 형식

**제목**: [CGV 알림] {영화명} 예매 오픈!

**본문**:
```
안녕하세요,

CGV 용산 IMAX에서 다음 영화의 예매가 오픈되었습니다!

🎬 영화: {영화 제목}
📍 극장: CGV 용산 IMAX
🎫 예매 링크: {예매 URL}

지금 바로 예매하세요!

---
CGV 예매 알림 시스템
감지 시간: {YYYY-MM-DD HH:MM:SS}
```

## 9. 개발 단계

### Phase 1: MVP (최소 기능 제품)
- [ ] 기본 웹 UI (영화 목록 표시)
- [ ] CGV 크롤러 기본 구현
- [ ] 이메일 알림 기능
- [ ] 간단한 대시보드
- [ ] SQLite 데이터베이스

**예상 기간**: 1-2주

### Phase 2: 핵심 기능 완성
- [ ] 영화 선택/해제 기능
- [ ] 자동 모니터링 시스템
- [ ] 알림 히스토리
- [ ] 설정 페이지
- [ ] 실시간 상태 업데이트

**예상 기간**: 1-2주

### Phase 3: 개선 및 배포
- [ ] UI/UX 개선
- [ ] 에러 처리 강화
- [ ] 로깅 시스템
- [ ] PostgreSQL 마이그레이션
- [ ] 웹서버 배포
- [ ] 도메인 및 SSL 설정

**예상 기간**: 1주

### Phase 4: 추가 기능 (선택)
- [ ] 다중 극장 지원
- [ ] 사용자 인증
- [ ] 모바일 앱 (PWA)
- [ ] 텔레그램/카카오톡 알림 추가
- [ ] 예매 자동화 (법적 검토 필요)

**예상 기간**: 2-4주

## 10. 고려사항 및 제약

### 10.1 법적/윤리적 고려사항
- CGV 이용약관 및 robots.txt 준수
- 과도한 크롤링으로 인한 서버 부하 방지
- 개인 사용 목적으로만 사용
- 상업적 이용 금지

### 10.2 기술적 제약
- CGV 웹사이트 구조 변경 시 크롤러 수정 필요
- IP 차단 위험 (적절한 간격 유지 필요)
- 동적 페이지 로딩 시 Selenium 필요 (속도 저하)
- 이메일 발송 제한 (일일 발송량)

### 10.3 보안
- 이메일 계정 정보 암호화 저장
- 환경변수로 민감 정보 관리
- HTTPS 필수
- CSRF 토큰 사용
- SQL Injection 방지

### 10.4 성능
- 크롤링 간격: 최소 5분 권장
- 데이터베이스 인덱싱
- 캐싱 전략 (영화 목록 등)
- 비동기 처리 (크롤링, 이메일 발송)

## 11. 성공 지표

- [ ] 예매 오픈 감지 정확도: 95% 이상
- [ ] 알림 발송 지연시간: 5분 이내
- [ ] 시스템 가동률: 99% 이상
- [ ] 이메일 전송 성공률: 95% 이상
- [ ] 크롤링 성공률: 90% 이상

## 12. 리스크 및 대응 방안

| 리스크 | 영향도 | 대응 방안 |
|--------|--------|-----------|
| CGV 웹사이트 구조 변경 | 높음 | 모니터링 및 빠른 업데이트, 에러 알림 |
| IP 차단 | 중간 | 적절한 간격 유지, User-Agent 설정, 프록시 고려 |
| 이메일 발송 실패 | 중간 | 재시도 로직, 대체 이메일 서비스 |
| 서버 다운타임 | 높음 | 헬스 체크, 자동 재시작, 모니터링 알림 |
| 데이터베이스 오류 | 중간 | 정기 백업, 트랜잭션 관리 |

## 13. 참고 자료

- CGV 웹사이트: https://www.cgv.co.kr/
- 크롤링 관련 법률: 저작권법, 정보통신망법
- 이메일 발송: Gmail SMTP, SendGrid API
- 배포 가이드: Flask/FastAPI 프로덕션 배포 문서

## 14. 향후 확장 가능성

1. **다른 극장 체인 지원**: 메가박스, 롯데시네마
2. **특정 좌석 알림**: 원하는 좌석 구역이 남았을 때만 알림
3. **가격 추적**: 할인 이벤트, 조조 할인 시간대 알림
4. **단체 관람 매칭**: 같은 영화 보고 싶은 사람끼리 연결
5. **모바일 앱**: React Native로 iOS/Android 앱 개발
6. **AI 추천**: 선호도 기반 영화 추천

---

**문서 버전**: 1.0
**작성일**: 2025-11-08
**최종 수정일**: 2025-11-08
